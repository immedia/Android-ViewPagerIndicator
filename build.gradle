buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        def isRootProject = rootProject == project
        if (isRootProject) {
            classpath 'com.android.tools.build:gradle:3.0.1'
            // + other dependencies
        }
    }


}

